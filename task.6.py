# — — — — — — — Задание №6 — — — — — —
a = []
b = int(input('введите возраст'))
a.append(b)

c = input('введите пол')
a.append(c)

d = input('введите имя')
a.append(d)

e = input('введите город')
a.append(e)

d = int(input('введите класс'))
a.append(d)

print(a)


